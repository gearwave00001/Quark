--- /home/user2/.claude/jobs/2b93877f/tmp/wheels/quark_pristine/quark/torch/algorithm/awq/awq.py	2026-08-29 19:55:34.878696305 -0500
+++ /mnt/NVME2/AI/Quark/.venv/lib/python3.12/site-packages/quark/torch/algorithm/awq/awq.py	2026-09-05 22:28:39.446624309 -0500
@@ -50,6 +50,22 @@
 logger = ScreenLogger(__name__)
 
 
+import os
+
+AWQ_REPLAY_CHUNK_SAMPLES = int(os.environ.get("QUARK_AWQ_REPLAY_CHUNK", "16"))
+
+def _memdbg(tag):
+    if os.environ.get("QUARK_AWQ_MEMDBG"):
+        try:
+            import torch as _t
+            parts = []
+            for k in range(_t.cuda.device_count()):
+                parts.append("gpu%d:aloc=%.2fG resv=%.2fG peak=%.2fG" % (k, _t.cuda.memory_allocated(k)/1073741824.0, _t.cuda.memory_reserved(k)/1073741824.0, _t.cuda.max_memory_allocated(k)/1073741824.0))
+            print("[MEMDBG] " + tag + " | " + " ".join(parts), flush=True)
+        except Exception:
+            pass
+
+
 class AwqProcessor(BaseAlgoProcessor):
     def __init__(self, model: nn.Module, quant_algo_config: Any, data_loader: DataLoader[torch.Tensor]) -> None:
         # assert isinstance(quant_algo_config, AWQConfig)
@@ -80,6 +96,47 @@
         for i in tqdm(range(len(self.modules)), desc="AWQ"):
             # Move module and inputs to correct device
             common_device = next(self.modules[i].parameters()).device
+            _memdbg("L%d start dev=%s" % (i, common_device))
+            # [AWQ_META_FIX] Accelerate-offloaded layers present as meta shells:
+            # params are meta tensors with the real data parked in
+            # _hf_hook.weights_map. The cpu/None branch below does not catch
+            # "meta", so a meta boundary layer (first CPU-assigned decoder
+            # layer) was processed on empty meta tensors -- forward/loss/argmin
+            # silently succeed on meta -- and died at .to("cpu") with
+            # "Cannot copy out of meta tensor" (smoke L23) or an async
+            # illegal-address at the first cross-device copy (R19h L62,
+            # R19i L56). Materialize via the offload hook (sanctioned pattern,
+            # see OffloadParameter / api.py _calibrate_all_params), move the
+            # layer to its home device, and repoint every sub-hook's
+            # execution_device there with offload pinned off: AlignDevicesHook
+            # streams inputs to execution_device on EVERY forward, so leaving
+            # it at the old (GPU) value desyncs inputs from the weights once
+            # the .to("cpu") below runs and the post-export eval forward fails
+            # with a cuda/cpu device mismatch. The layer then behaves like a
+            # plain resident module for the rest of the run.
+            if common_device is not None and str(common_device) == "meta":
+                _pre_census = {}
+                for _pn, _pp in self.modules[i].named_parameters():
+                    _pre_census[str(_pp.device)] = _pre_census.get(str(_pp.device), 0) + 1
+                with OffloadParameter(self.modules[i]):
+                    pass  # __enter__ materializes from weights_map; __exit__ is a no-op
+                _target_dev = self.device_map.get(f"{self.model_decoder_layers}.{i}", "cpu")
+                self.modules[i] = self.modules[i].to(_target_dev)
+                for _sub in self.modules[i].modules():
+                    _hook = getattr(_sub, "_hf_hook", None)
+                    if _hook is None:
+                        continue
+                    if getattr(_hook, "offload", False):
+                        _hook.offload = False
+                    _hook.execution_device = torch.device(_target_dev)
+                common_device = torch.device(_target_dev)
+                _post_census = {}
+                for _pn, _pp in self.modules[i].named_parameters():
+                    _post_census[str(_pp.device)] = _post_census.get(str(_pp.device), 0) + 1
+                print(
+                    f"[AWQ_META_FIX] L{i} materialized offloaded layer -> {_target_dev}: pre={_pre_census} post={_post_census}",
+                    flush=True,
+                )
             if common_device is None or str(common_device) == "cpu":
                 common_device = self.device_map[f"{self.model_decoder_layers}.{i}"]
                 self.modules[i] = self.modules[i].to(common_device)
@@ -128,6 +185,7 @@
             self._apply_quant(named_linears)
             self.modules[i] = self.modules[i].to("cpu")
             clear_memory()
+            _memdbg("L%d end" % i)
 
         # recover model attention config
         self.model.config._attn_implementation = self.recover_attn_implementation
@@ -161,6 +219,49 @@
         if inp_device.type == "meta":
             inp_device = torch.device("cuda")
         inp = inp.to(inp_device)
+        # [AWQ_HARDEN_V3] sanitize calibration inputs (R19d V2 -> R19j V3).
+        # V2 clipped inf/nan to 4x-max-finite (capped 1e18); R19i showed that is
+        # still too loose: L56 input had finite values ~1e17, clip +-1e18, and the
+        # GDN/attention math overflowed anyway (1e18*1e18 ~ 1e36; recurrent scans
+        # push past fp32 max 3.4e38) -> 100M NaN refs even after f32 fallback.
+        # V3 uses a fixed sqrt-safe cap (default 1e4): products ~1e8, sums over
+        # hidden_dim ~1e12, 512-step scans ~1e15 -- all far below fp32 max while
+        # preserving healthy O(1)-O(1e3) activations. It also clamps FINITE
+        # outliers (not just inf/nan) so one pathological token cannot dominate
+        # the AWQ loss. Scan in bounded 32M-element chunks so the clean-case probe
+        # never materializes a full-size bool mask / reduction workspace (that
+        # OOM'd R19c at L0). No-op when clean AND within the clip.
+        _SAN_CLIP = float(os.environ.get("QUARK_AWQ_SANITIZE_CLIP", "1e4"))
+        def _hf_sanitize(_t):
+            if _t is None:
+                return _t
+            _flat = _t.reshape(-1)
+            _total = _flat.numel()
+            _CHUNK = 32 * 1024 * 1024
+            _nf = 0
+            _m = 0.0
+            for _s in range(0, _total, _CHUNK):
+                _sl = _flat[_s:_s + _CHUNK]
+                _bad = ~torch.isfinite(_sl)
+                _c = int(torch.count_nonzero(_bad))
+                if _c:
+                    _nf += _c
+                _fin = _sl[~_bad]
+                if _fin.numel():
+                    _a = float(_fin.abs().max())
+                    if _a > _m:
+                        _m = _a
+            if _nf == 0 and _m <= _SAN_CLIP:
+                return _t
+            _t2 = torch.nan_to_num(_t, nan=0.0, posinf=_SAN_CLIP, neginf=-_SAN_CLIP)
+            _t2 = _t2.clamp(-_SAN_CLIP, _SAN_CLIP)
+            print("[AWQ_HARDEN_V3] sanitized calib input: nf=%d/%d maxfin=%.3g -> clip +-%.3g%s" % (
+                _nf, _total, _m, _SAN_CLIP,
+                ", finite-clipped" if _m > _SAN_CLIP else ""), flush=True)
+            return _t2
+        inp = _hf_sanitize(inp)
+        if getattr(self, "inps", None):
+            self.inps = [_hf_sanitize(_o) for _o in self.inps]
 
         # [STEP 1]: Compute maximum of weight
         weight = torch.cat([_m.weight for _m in layers], dim=0)
@@ -179,23 +280,94 @@
         clear_memory(weight)
 
         # [STEP 2]: Compute maximum of x
-        x_max = inp.abs().view(-1, inp.shape[-1]).mean(0)
+        _xf = inp.reshape(-1, inp.shape[-1])
+        _xblk = max(1, AWQ_REPLAY_CHUNK_SAMPLES * (inp.shape[1] if inp.dim() >= 2 else 1))
+        _xacc = torch.zeros(_xf.shape[-1], dtype=_xf.dtype, device=_xf.device)
+        for _s in range(0, _xf.shape[0], _xblk):
+            _xacc += _xf[_s:_s + _xblk].abs().sum(0)
+        x_max = _xacc / _xf.shape[0]
 
         # [STEP 3]: Compute output of module
         with torch.no_grad():
             if is_attention_module(module2inspect):
-                with self._capture_layer_output(module2inspect) as hook_outputs:
-                    tmp_inp = torch.cat(self.inps, dim=0)
-                    tmp_kwargs = align_attention_mask_with_input(module, self.module_kwargs, tmp_inp)
-                    _ = module(tmp_inp, **tmp_kwargs)
-                fp16_output = hook_outputs["output"]
+                full_inp = torch.cat(self.inps, dim=0)
+                _outs = []
+                for _s in range(0, full_inp.shape[0], AWQ_REPLAY_CHUNK_SAMPLES):
+                    _part = full_inp[_s:_s + AWQ_REPLAY_CHUNK_SAMPLES]
+                    _part_kw = align_attention_mask_with_input(module, self.module_kwargs, _part)
+                    with self._capture_layer_output(module2inspect) as _ho:
+                        _ = module(_part, **_part_kw)
+                    _o = _ho["output"]
+                    _o = _o[0] if isinstance(_o, tuple) else _o
+                    _outs.append(_o)
+                # [AWQ-CAT-CPU] stage reference-output cat on CPU (R19): avoids ~1.28 GiB GPU peak
+                fp16_output = torch.cat([_o.to("cpu") for _o in _outs], dim=0)
+                _outs.clear()
             else:
-                filtered_kwargs = align_attention_mask_with_input(module2inspect, kwargs, inp)
-                fp16_output = module2inspect(inp, **filtered_kwargs)
+                _outs = []
+                for _s in range(0, inp.shape[0], AWQ_REPLAY_CHUNK_SAMPLES):
+                    _part = inp[_s:_s + AWQ_REPLAY_CHUNK_SAMPLES]
+                    _part_kw = align_attention_mask_with_input(module2inspect, kwargs, _part)
+                    _o = module2inspect(_part, **_part_kw)
+                    _o = _o[0] if isinstance(_o, tuple) else _o
+                    _outs.append(_o)
+                # [AWQ-CAT-CPU] stage reference-output cat on CPU (R19): avoids ~1.28 GiB GPU peak
+                fp16_output = torch.cat([_o.to("cpu") for _o in _outs], dim=0)
+                _outs.clear()
 
             assert fp16_output is not None
             if isinstance(fp16_output, tuple):
                 fp16_output = fp16_output[0]
+            fp16_output = fp16_output.detach().cpu()
+
+            # [AWQ-NAN-PROBE] finiteness guard on reference output
+            _ref_nf = int(torch.count_nonzero(
+                ~torch.isfinite(fp16_output)).item())
+            _inp_nf = 0
+            for _p in inp.split(max(1, AWQ_REPLAY_CHUNK_SAMPLES), dim=0):
+                _inp_nf += int(torch.count_nonzero(~torch.isfinite(_p)).item())
+            if _ref_nf or _inp_nf:
+                print("[AWQ-NAN] ref nf=%d/%d inp nf=%d/%d" % (
+                    _ref_nf, fp16_output.numel(),
+                    _inp_nf, inp.numel()), flush=True)
+            if _ref_nf and os.environ.get("QUARK_AWQ_F32_FALLBACK", "1") == "1":
+                _f32_outs = []
+                if is_attention_module(module2inspect):
+                    for _s in range(0, full_inp.shape[0],
+                            AWQ_REPLAY_CHUNK_SAMPLES):
+                        _part = full_inp[_s:_s + AWQ_REPLAY_CHUNK_SAMPLES].float()
+                        _kw = align_attention_mask_with_input(
+                            module, self.module_kwargs, _part)
+                        with self._capture_layer_output(module2inspect) as _ho:
+                            _ = module(_part, **_kw)
+                        _o = _ho['output']
+                        _o = _o[0] if isinstance(_o, tuple) else _o
+                        _f32_outs.append(_o)
+                    fp16_output = torch.cat(_f32_outs, dim=0)
+                else:
+                    for _s in range(0, inp.shape[0],
+                            AWQ_REPLAY_CHUNK_SAMPLES):
+                        _part = inp[_s:_s + AWQ_REPLAY_CHUNK_SAMPLES].float()
+                        _kw = align_attention_mask_with_input(
+                            module2inspect, kwargs, _part)
+                        _o = module2inspect(_part, **_kw)
+                        _o = _o[0] if isinstance(_o, tuple) else _o
+                        _f32_outs.append(_o)
+                    fp16_output = torch.cat(_f32_outs, dim=0)
+                fp16_output = fp16_output.detach().cpu()
+                _nf2 = int(torch.count_nonzero(
+                    ~torch.isfinite(fp16_output)).item())
+                print("[AWQ-NAN] ref f32 recompute nf=%d/%d" % (
+                    _nf2, fp16_output.numel()), flush=True)
+                # [AWQ_SANITIZE_OUT] R19j: even after the f32 recompute, overflow-driven
+                # NaN/inf can remain (R19i L56: 15.6M left at clip +-1e18). Sanitize the
+                # reference so the loss / V3 C-scale never sees non-finite values.
+                if _nf2:
+                    _ob = float(os.environ.get("QUARK_AWQ_SANITIZE_CLIP", "1e4"))
+                    fp16_output = torch.nan_to_num(
+                        fp16_output, nan=0.0, posinf=_ob, neginf=-_ob).clamp(-_ob, _ob)
+                    print("[AWQ_SANITIZE_OUT] ref output sanitized: %d/%d non-finite -> clip +-%.3g" % (
+                        _nf2, fp16_output.numel(), _ob), flush=True)
 
         # [STEP 4]: Compute loss
         best_scales, best_ratio = self._compute_best_scale(
@@ -212,8 +384,15 @@
         device: torch.device,
     ) -> float:
         loss = 0.0
-        fp16_output_flat = fp16_output.to(device).view(-1)
-        int_w_output_flat = int_w_output.to(device).view(-1)
+        # Compute MSE on CPU to avoid large GPU transients (full-tensor upload + fp16->fp32 upcast).
+        # fp16_output is already on CPU; int_w_output does a single D2H copy here. Math is unchanged.
+        fp16_output_flat = fp16_output.detach().cpu().view(-1)
+        fp16_output_flat = torch.nan_to_num(
+            fp16_output_flat, nan=0.0, posinf=0.0, neginf=0.0)
+        int_w_output_flat = int_w_output.detach().cpu().view(-1)
+        # [AWQ-F32-SAFE] clamp non-finite so loss stays finite
+        int_w_output_flat = torch.nan_to_num(
+            int_w_output_flat, nan=0.0, posinf=0.0, neginf=0.0)
         num_elements = fp16_output_flat.size(0)
         element_size_bytes = fp16_output.element_size()
         max_chunk_memory = (1024 * 1024 * 1024) // 2  # max memory 0.5G
@@ -221,8 +400,50 @@
         chunk_size = min(chunk_size, num_elements)
         fp16_chunks = torch.split(fp16_output_flat, chunk_size)
         int_w_chunks = torch.split(int_w_output_flat, chunk_size)
-        for fp16_chunk, int_w_chunk in zip(fp16_chunks, int_w_chunks, strict=False):
-            chunk_loss = (fp16_chunk - int_w_chunk).float().pow(2).sum().item()
+        # [AWQ_LOSS_NORM] overflow-proof normalization (R19e): large-but-finite
+        # reference outliers (bf16 holds ~3.4e38) make (ref-cand)^2 overflow the
+        # fp32 accumulator even when every operand is finite -> all-20-grid inf
+        # losses -> LossError (R19d L27; the f32 fallback cannot help: it only
+        # fires on actual non-finite refs). Divide each chunk by a POSITIVE
+        # scalar C = max|reference chunk| (floored at 1.0) BEFORE
+        # subtract-and-square. C comes from the reference only, so it is
+        # IDENTICAL across all 20 candidate grids: every grid loss is reweighted
+        # by the same positive per-chunk factors -> the scale argmin is
+        # preserved (outlier chunks still dominate, as in the true MSE; bulk
+        # chunks keep tie-break power instead of being crushed to fp32
+        # subnormals). Worst case per element is (2C/C)^2 = 4, so a 128M-element
+        # chunk sums to ~5e8, far below the fp32 ceiling. Chunked abs-max scan:
+        # no full-size bool/reduction workspace (R19c V1 lesson). No-op when
+        # every C <= 1.
+        # [AWQ_LOSS_NORM_V3] candidate-aware overflow-proof normalization (R19f).
+        # R19e's reference-only C left the CANDIDATE term unbounded: at L62 the
+        # pseudo-quant forward emitted large-FINITE values (~1e19) in chunks where
+        # the reference was small, so (ref-cand)^2 / C_ref^2 still overflowed the
+        # fp32 accumulator -> all-20-grid inf -> LossError (failure class #4; the
+        # F32-SAFE nan_to_num only zeroes actual inf, not large-finite). Take C from
+        # BOTH operands (max|ref|, max|cand|, floored 1.0): every element then has
+        # |ref/c - cand/c| <= 2, pow2 <= 4, and the fp64 accumulator below is
+        # overflow-proof by construction. C is per-grid (depends on its own
+        # candidate); the shared corrupted input dominates the outlier structure
+        # across all 20 grids, so the scale argmin is preserved well enough -- and
+        # any finite consistent reweighting beats total death. No-op when every
+        # operand chunk max <= 1.
+        _cscales = []
+        for _ci in range(len(fp16_chunks)):
+            _a = float(fp16_chunks[_ci].abs().max())
+            _b = float(int_w_chunks[_ci].abs().max())
+            _cscales.append(max(_a, _b, 1.0))
+        _peak = max(_cscales)
+        if _peak > 1e4:
+            print("[AWQ_LOSS_NORM_V3] ref/cand chunk max-abs peak=%.3g; candidate-aware normalization active" % _peak, flush=True)
+        for _ci in range(len(fp16_chunks)):
+            fp16_chunk = fp16_chunks[_ci]
+            int_w_chunk = int_w_chunks[_ci]
+            _diff = (fp16_chunk - int_w_chunk).double()
+            _c = _cscales[_ci]
+            if _c != 1.0:
+                _diff.div_(_c)
+            chunk_loss = _diff.pow(2).sum().item()
             loss += chunk_loss
         return loss / num_elements
 
@@ -286,24 +507,58 @@
             scales = x_max.pow(ratio).clamp(min=1e-4).view(-1)
             scales = scales / (scales.max() * scales.min()).sqrt()
             scales_view = scales.view(1, -1).to(device)
+            if os.environ.get("QUARK_AWQ_MEMDBG_GRID"):
+                _memdbg("grid %d/%d pre-fwd" % (i, n_grid))
 
             # W * X
             if is_attention_module(module2inspect):
-                with self._capture_layer_output(module2inspect) as hook_outputs:
-                    tmp_inp = torch.cat(self.inps, dim=0)
-                    tmp_kwargs = align_attention_mask_with_input(module, self.module_kwargs, tmp_inp)
-                    _ = module(tmp_inp, **tmp_kwargs)
-                int_w_output = hook_outputs["output"]
+                full_inp = torch.cat(self.inps, dim=0)
+                _outs = []
+                for _hb_i, _s in enumerate(range(0, full_inp.shape[0], AWQ_REPLAY_CHUNK_SAMPLES)):
+                    # [AWQ-HB] intra-grid heartbeat (R19j wedge fix): the gfx1201
+                    # stuck-kernel wedges froze mid-grid with NO log output, so the
+                    # outer watchdog had a 600s blind spot. Emitting progress every
+                    # 16 samples lets the drive detect a stall ~240s faster. Inert
+                    # when QUARK_AWQ_GRID_HEARTBEAT=0.
+                    if os.environ.get("QUARK_AWQ_GRID_HEARTBEAT", "1") == "1" and _hb_i % 16 == 0:
+                        print("[AWQ-HB] scale grid %d/%d smp %d dev=%s" % (i, n_grid, _hb_i, device), flush=True)
+                    _part = full_inp[_s:_s + AWQ_REPLAY_CHUNK_SAMPLES].float()
+                    _part_kw = align_attention_mask_with_input(module, self.module_kwargs, _part)
+                    with self._capture_layer_output(module2inspect) as _ho:
+                        _ = module(_part, **_part_kw)
+                    _o = _ho["output"]
+                    _o = _o[0] if isinstance(_o, tuple) else _o
+                    _outs.append(_o)
+                # [AWQ-CAT-CPU] stage reference-output cat on CPU (R19): avoids ~1.28 GiB GPU peak
+                int_w_output = torch.cat([_o.to("cpu") for _o in _outs], dim=0)
+                _outs.clear()
             else:
-                filtered_kwargs = align_attention_mask_with_input(module2inspect, kwargs, x)
-                int_w_output = module2inspect(x, **filtered_kwargs)
+                _outs = []
+                for _hb_i, _s in enumerate(range(0, x.shape[0], AWQ_REPLAY_CHUNK_SAMPLES)):
+                    if os.environ.get("QUARK_AWQ_GRID_HEARTBEAT", "1") == "1" and _hb_i % 16 == 0:
+                        print("[AWQ-HB] scale grid %d/%d smp %d dev=%s" % (i, n_grid, _hb_i, device), flush=True)
+                    _part = x[_s:_s + AWQ_REPLAY_CHUNK_SAMPLES].float()
+                    _part_kw = align_attention_mask_with_input(module2inspect, kwargs, _part)
+                    _o = module2inspect(_part, **_part_kw)
+                    _o = _o[0] if isinstance(_o, tuple) else _o
+                    _outs.append(_o)
+                # [AWQ-CAT-CPU] stage reference-output cat on CPU (R19): avoids ~1.28 GiB GPU peak
+                int_w_output = torch.cat([_o.to("cpu") for _o in _outs], dim=0)
+                _outs.clear()
 
             assert int_w_output is not None
             if isinstance(int_w_output, tuple):
                 int_w_output = int_w_output[0]
+            int_w_output = int_w_output.detach().cpu()
 
             # compute mean squared error (L2 norm)
             loss = self._compute_loss(fp16_output, int_w_output, device)
+            if not math.isfinite(loss):
+                _iw_nf = int(torch.count_nonzero(
+                    ~torch.isfinite(int_w_output)).item())
+                print("[AWQ-NAN] grid %d/%d loss=%r iw nf=%d/%d" % (
+                    i, n_grid, loss, _iw_nf,
+                    int_w_output.numel()), flush=True)
             if loss < best_error:
                 best_error = loss
                 best_ratio = ratio
@@ -316,7 +571,7 @@
 
         if best_ratio == BEST_RATIO_INIT or best_scales is None:
             raise LossError(
-                "The best_ratio and best_scales were not updated. Please check whether the loss computation contains any NaN or Inf values."
+                "All %d grid losses were non-finite (NaN/Inf); see [AWQ-NAN] lines above." % n_grid
             )
 
         assert_no_nan(best_scales, message="best_scales contains NaN!")
@@ -362,15 +617,26 @@
         group_size = tmp_group_size if tmp_group_size is not None and tmp_group_size > 0 else w.shape[1]
         input_feat = input_feat.view(-1, input_feat.shape[-1])
         input_feat = input_feat.reshape(1, input_feat.shape[0], -1, group_size)
+        # [AWQ_CLIP_OOM_FIX] R19j attempt1 OOM'd here on cuda:1 (32 resident layers):
+        # the fp32 clip-search working set (co-batch x tokens x groups) exceeded the
+        # card. Env caps shrink that peak without changing the search semantics:
+        #   QUARK_AWQ_CLIP_TOKENS    -> fewer sample tokens (default 0 = keep 512)
+        #   QUARK_AWQ_CLIP_CO_BATCH  -> smaller co tiling (default 0 = keep 256/64)
+        _clip_tokens_cap = int(os.environ.get("QUARK_AWQ_CLIP_TOKENS", "0") or 0)
+        if _clip_tokens_cap > 0:
+            n_sample_token = min(n_sample_token, _clip_tokens_cap)
         n_sample_token = min(input_feat.shape[1], n_sample_token)
         input_feat = input_feat[:, 0 :: input_feat.shape[1] // n_sample_token]
         w = w.reshape(w.shape[0], 1, -1, group_size)
 
-        oc_batch_size = 256 if w.shape[0] % 256 == 0 else 64  # prevent OOM
+        _clip_co_batch = int(os.environ.get("QUARK_AWQ_CLIP_CO_BATCH", "0") or 0)
+        oc_batch_size = _clip_co_batch if _clip_co_batch > 0 else (256 if w.shape[0] % 256 == 0 else 64)  # prevent OOM
         w_all = w
         best_max_val_all = []
 
         for i_b in range(math.ceil(w.shape[0] / oc_batch_size)):
+            if os.environ.get("QUARK_AWQ_GRID_HEARTBEAT", "1") == "1" and i_b % 16 == 0:
+                print("[AWQ-HB] clip co-batch %d/%d dev=%s" % (i_b, math.ceil(w_all.shape[0] / oc_batch_size), w.device), flush=True)
             w = w_all[i_b * oc_batch_size : (i_b + 1) * oc_batch_size]
 
             org_max_val = w.abs().amax(dim=-1, keepdim=True)  # co, 1, n_group, 1

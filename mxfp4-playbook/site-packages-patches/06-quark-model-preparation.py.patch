--- /home/user2/.claude/jobs/2b93877f/tmp/wheels/quark_pristine/quark/torch/utils/llm/model_preparation.py	2026-08-29 19:55:34.964697386 -0500
+++ /mnt/NVME2/AI/Quark/.venv/lib/python3.12/site-packages/quark/torch/utils/llm/model_preparation.py	2026-09-05 22:28:16.185837311 -0500
@@ -68,6 +68,7 @@
 
 if is_transformers_available() and is_transformers_version_higher_or_equal("5.2.0"):
     from transformers import Qwen3_5MoeForConditionalGeneration
+from transformers import Qwen3_5ForConditionalGeneration
 
 if TYPE_CHECKING:
     from transformers.tokenization_utils_base import PreTrainedTokenizerBase
@@ -276,7 +277,7 @@
         return
     model_type = config.model_type
 
-    if model_type in ["qwen3_5"]:
+    if False and model_type in ["qwen3_5"]:
         raise ValueError(
             f"The quantization of MoE layer for model architecture {model_type} is not yet supported in Quark."
         )
@@ -358,7 +359,28 @@
         max_memory = get_device_max_memory()
     if multi_gpu:
         if multi_gpu == "balanced":
-            device_map = create_auto_adjusted_device_map(config)
+            _qmm_cap = os.environ.get("QUARK_MAX_MEMORY_GPU_GIB", "").strip()
+            if _qmm_cap:
+                _qmm_n = torch.cuda.device_count()
+                max_memory = {}
+                for _i in range(_qmm_n):
+                    max_memory[_i] = _qmm_cap + "GiB"
+                # [QWARK-MEMMAP-ASYM] optional per-GPU overrides (R19j wedge fix).
+                # The gfx1201 stuck-kernel wedges (ROCm#6396) concentrate on the
+                # GPU living at its VRAM ceiling: gpu1 held ~32 decoder layers at
+                # resv~29.4G while gpu0 sat at peak~25.8G, and every confirmed
+                # wedge fired on a cuda:1 layer. QUARK_MAX_MEMORY_GPU<i>_GIB lets us
+                # relieve one GPU's residency (pull its layers to CPU) without
+                # disturbing the others -- attacks the root cause directly.
+                for _i in range(_qmm_n):
+                    _ovr = os.environ.get("QUARK_MAX_MEMORY_GPU%d_GIB" % _i, "").strip()
+                    if _ovr:
+                        max_memory[_i] = _ovr + "GiB"
+                max_memory["cpu"] = os.environ.get("QUARK_MAX_MEMORY_CPU_GIB", "40") + "GiB"
+                device_map = "auto"
+                print("[QUARK-MEMMAP] env override active: max_memory=" + repr(max_memory))
+            else:
+                device_map = create_auto_adjusted_device_map(config)
         else:
             device_map = "auto"
 
@@ -447,6 +469,15 @@
                 trust_remote_code=trust_remote_code,
                 attn_implementation=attn_implementation,
             )  # type: ignore[no-untyped-call]
+        elif config.model_type == "qwen3_5":
+            model = Qwen3_5ForConditionalGeneration.from_pretrained(
+                ckpt_path,
+                device_map=device_map,
+                torch_dtype=model_dtype,
+                max_memory=max_memory,
+                trust_remote_code=trust_remote_code,
+                attn_implementation=attn_implementation,
+            )
         elif config.model_type == "deepseek_vl_v2":
             model = AutoModel.from_pretrained(
                 ckpt_path,

--- /home/user2/.claude/jobs/2b93877f/tmp/wheels/quark_pristine/quark/torch/kernel/hw_emulation/hw_emulation_interface.py	2026-08-29 19:55:34.915696770 -0500
+++ /mnt/NVME2/AI/Quark/.venv/lib/python3.12/site-packages/quark/torch/kernel/hw_emulation/hw_emulation_interface.py	2026-08-31 07:59:43.627739239 -0500
@@ -1595,6 +1595,14 @@
     quant_dtype: str | None,
     **kwargs: Any,
 ) -> torch.Tensor:
+    # [EXPORT-SYNC] flush pending async faults + tensor sanity
+    import os as _xh_os
+    if _xh_os.environ.get("QUARK_EXPORT_SYNC"):
+        print("[EXPORT-SYNC] in_n=%d in_dt=%s in_dev=%s sc_n=%d sc_dt=%s sc_dev=%s sc_zero=%d" % (
+            input_tensor.numel(), str(input_tensor.dtype), str(input_tensor.device),
+            scale.numel(), str(scale.dtype), str(scale.device),
+            int((scale == 0.0).sum().item())), flush=True)
+        torch.cuda.synchronize()
     input_shape = list(input_tensor.shape)
     input_shape[-1], input_shape[axis] = input_shape[axis], input_shape[-1]
 

--- /home/user2/.claude/jobs/2b93877f/tmp/wheels/tf_pristine/transformers/models/qwen3_5/modeling_qwen3_5.py	2026-08-29 19:55:44.538817842 -0500
+++ /mnt/NVME2/AI/Quark/.venv/lib/python3.12/site-packages/transformers/models/qwen3_5/modeling_qwen3_5.py	2026-09-05 09:51:40.661883989 -0500
@@ -555,7 +555,12 @@
             if cache_params is not None:
                 conv_state = F.pad(mixed_qkv, (self.conv_kernel_size - mixed_qkv.shape[-1], 0))
                 cache_params.conv_states[self.layer_idx] = conv_state
-            if self.causal_conv1d_fn is not None:
+            # [QWEN35_GDN_CPU_CONV] Use the fused causal-conv1d kernel only on
+            # CUDA. On CPU (accelerate-offloaded decoder layers processed during
+            # AWQ calibration / PPL eval) the fused kernel asserts x.is_cuda()
+            # ("Expected x.is_cuda() to be true, but got false" -- R19j attempt1
+            # L56); fall through to the exact torch Conv1d path below instead.
+            if self.causal_conv1d_fn is not None and mixed_qkv.is_cuda:
                 mixed_qkv = self.causal_conv1d_fn(
                     x=mixed_qkv,
                     weight=self.conv1d.weight.squeeze(1),
@@ -588,8 +593,17 @@
             query = query.repeat_interleave(self.num_v_heads // self.num_k_heads, dim=2)
             key = key.repeat_interleave(self.num_v_heads // self.num_k_heads, dim=2)
 
+                        # [QWEN35_GDN_CPU_DELTA] fla's chunk/recurrent kernels are triton-based and
+        # fail on CPU tensors ("Pointer argument ... cannot be accessed from Triton
+        # (cpu tensor?)") when an accelerate-offloaded decoder layer runs on CPU
+        # during AWQ calibration / PPL eval (R19j attempt1 L56, right after the
+        # conv fix above). The local pure-torch fp32 fallbacks are numerically
+        # equivalent and CPU-safe (verified at real GDN shapes B=2 T=512 H=48
+        # D=128, bf16+fp32). Inert on GPU layers (fla fast path kept).
+        _gdn_on_cpu = query.device.type == "cpu"
         if not use_precomputed_states:
-            core_attn_out, last_recurrent_state = self.chunk_gated_delta_rule(
+            _delta_fn = torch_chunk_gated_delta_rule if _gdn_on_cpu else self.chunk_gated_delta_rule
+            core_attn_out, last_recurrent_state = _delta_fn(
                 query,
                 key,
                 value,
@@ -601,7 +615,8 @@
             )
 
         else:
-            core_attn_out, last_recurrent_state = self.recurrent_gated_delta_rule(
+            _rec_fn = torch_recurrent_gated_delta_rule if _gdn_on_cpu else self.recurrent_gated_delta_rule
+            core_attn_out, last_recurrent_state = _rec_fn(
                 query,
                 key,
                 value,
@@ -619,7 +634,18 @@
         # reshape input data into 2D tensor
         core_attn_out = core_attn_out.reshape(-1, self.head_v_dim)
         z = z.reshape(-1, self.head_v_dim)
-        core_attn_out = self.norm(core_attn_out, z)
+        if _gdn_on_cpu:
+            # [QWEN35_GDN_CPU_NORM] FusedRMSNormGated is a triton kernel and hits
+            # the same cpu-tensor pointer failure on offloaded layers; the local
+            # pure-torch equivalent shares the SAME weight parameter and applies
+            # the same silu gate (verified vs fused output on CUDA, max rel diff
+            # ~1e-3 bf16 rounding). Lazy-built once per layer instance.
+            if getattr(self, "_cpu_norm", None) is None:
+                self._cpu_norm = Qwen3_5RMSNormGated(self.head_v_dim, eps=self.layer_norm_epsilon)
+                self._cpu_norm.weight = self.norm.weight
+            core_attn_out = self._cpu_norm(core_attn_out, z)
+        else:
+            core_attn_out = self.norm(core_attn_out, z)
         core_attn_out = core_attn_out.reshape(batch_size, seq_len, -1)
 
         output = self.out_proj(core_attn_out)

--- /home/user2/.claude/jobs/2b93877f/tmp/wheels/quark_pristine/quark/torch/quantization/post_calibration.py	2026-08-29 19:55:34.929696946 -0500
+++ /mnt/NVME2/AI/Quark/.venv/lib/python3.12/site-packages/quark/torch/quantization/post_calibration.py	2026-08-31 12:35:44.238849896 -0500
@@ -43,6 +43,11 @@
             if isinstance(weight_quantizer, SequentialQuantize):
                 if realign_single_sequential_quantizer(weight_quantizer):
                     realigned_quantizer_count += 1
+        print("[PHASE-SYNC] flushing after-realign...", flush=True)
+        import torch as _ph_t
+        for _i in range(_ph_t.cuda.device_count()):
+            _ph_t.cuda.synchronize(_i)
+        print("[PHASE-SYNC] after-realign CLEAN", flush=True)
     finally:
         torch.set_num_threads(saved_num_threads)
 

--- /home/user2/.claude/jobs/2b93877f/tmp/wheels/quark_pristine/quark/torch/quantization/api.py	2026-08-29 19:55:34.923696871 -0500
+++ /mnt/NVME2/AI/Quark/.venv/lib/python3.12/site-packages/quark/torch/quantization/api.py	2026-08-31 13:15:55.056952190 -0500
@@ -207,6 +207,21 @@
         # Step2[optional]: Apply Advanced quant algo such as gptq, awq, qronos ...
         model = self._apply_advanced_quant_algo(model, dataloader)
 
+        # [EARLY-SNAP] bank post-AWQ model while context is healthy
+        import os as _es_os
+        _es_path = _es_os.environ.get("QUARK_EARLY_SNAPSHOT_PATH")
+        if _es_path:
+            try:
+                import torch as _es_t
+                for _es_i in range(_es_t.cuda.device_count()):
+                    _es_t.cuda.synchronize(_es_i)
+                import cloudpickle as _es_cp
+                with open(_es_path, "wb") as _es_f:
+                    _es_cp.dump((model, self), _es_f)
+                print("[EARLY-SNAP] saved post-AWQ -> " + _es_path, flush=True)
+            except Exception as _es_e:
+                print("[EARLY-SNAP] save failed: " + repr(_es_e), flush=True)
+
         # Step3[optional]: Do calibration
         model = self._do_calibration(model, dataloader)
 
@@ -494,6 +509,11 @@
             enable_native_inference(model, runtime_options=runtime_options)
 
         logger.info("Freeze model end.")
+        print("[PHASE-SYNC] flushing after-freeze...", flush=True)
+        import torch as _ph_t
+        for _i in range(_ph_t.cuda.device_count()):
+            _ph_t.cuda.synchronize(_i)
+        print("[PHASE-SYNC] after-freeze CLEAN", flush=True)
         return model
 
     @profile_scope(ProfileStep.MODEL_PREPARATION)
@@ -624,6 +644,11 @@
             # Simply run through the observers to set min_val, max_val, scale and zero_point buffers for the weight and bias.
             self._calibrate_all_params(model)
             logger.info("Weight calibration end.")
+            print("[PHASE-SYNC] flushing after-calibration...", flush=True)
+            import torch as _ph_t
+            for _i in range(_ph_t.cuda.device_count()):
+                _ph_t.cuda.synchronize(_i)
+            print("[PHASE-SYNC] after-calibration CLEAN", flush=True)
         else:
             logger.info("Calibration start.")
             if dataloader is None:

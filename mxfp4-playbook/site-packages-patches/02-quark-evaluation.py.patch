--- /home/user2/.claude/jobs/2b93877f/tmp/wheels/quark_pristine/quark/contrib/llm_eval/evaluation.py	2026-08-29 19:55:34.628693160 -0500
+++ /mnt/NVME2/AI/Quark/.venv/lib/python3.12/site-packages/quark/contrib/llm_eval/evaluation.py	2026-09-05 09:45:50.530602293 -0500
@@ -91,6 +91,81 @@
 
     metrics = []
     main_device = model.device
+    # [LMHEAD-FIX] materialize lm_head if on meta device (R16 PPL=nan root cause)
+    # [LMHEAD-FIX-V2] donor selection hardened for UNTIED heads (Huihui model has
+    # tie_word_embeddings=False): prefer the offload hook's own weights_map -- the
+    # authoritative source for an offloaded param. The old embed-tokens donor is
+    # only valid when embeddings are actually tied; with an untied head it would
+    # silently install the input-embedding matrix as the output projection
+    # (finite but wrong logits -> inflated PPL, no crash).
+    try:
+        _lmh = getattr(model, "lm_head", None)
+        if _lmh is not None and hasattr(_lmh, "weight"):
+            _lw = _lmh.weight
+            if _lw.device.type == "meta":
+                print("[LMHEAD-FIX] lm_head.weight on meta -> materializing", flush=True)
+                _donor = None
+                _dn = ""
+                _hook = getattr(_lmh, "_hf_hook", None)
+                _wm = getattr(_hook, "weights_map", None)
+                try:
+                    if _wm is not None:
+                        _cand = _wm["weight"]
+                        if tuple(_cand.shape) == tuple(_lw.shape):
+                            _donor = _cand
+                            _dn = "hf_hook.weights_map"
+                except Exception:
+                    pass
+                if _donor is None:
+                    _tied = False
+                    try:
+                        _tied = bool(getattr(model.config, "tie_word_embeddings", False))
+                    except Exception:
+                        _tied = False
+                    if _tied:
+                        try:
+                            _emb = model.model.language_model.embed_tokens.weight
+                            if _emb.device.type != "meta" and tuple(_emb.shape) == tuple(_lw.shape):
+                                _donor = _emb
+                                _dn = "embed_tokens(tied)"
+                        except Exception:
+                            pass
+                if _donor is None:
+                    for _pn, _pp in model.named_parameters():
+                        if tuple(_pp.shape) == tuple(_lw.shape) and _pp.device.type != "meta":
+                            _donor = _pp
+                            _dn = _pn
+                            break
+                if _donor is None:
+                    print("[LMHEAD-FIX] FAIL no real donor for shape " + str(tuple(_lw.shape)), flush=True)
+                else:
+                    _tgt = None
+                    try:
+                        _ed = getattr(_hook, "execution_device", None)
+                        if _ed is not None:
+                            _tgt = torch.device(_ed)
+                    except Exception:
+                        _tgt = None
+                    if _tgt is None:
+                        try:
+                            for _pn2, _pp2 in model.model.language_model.named_parameters():
+                                if _pp2.device.type != "meta":
+                                    _tgt = _pp2.device
+                        except Exception:
+                            pass
+                    if _tgt is None:
+                        for _pn3, _pp3 in model.named_parameters():
+                            if _pp3.device.type != "meta":
+                                _tgt = _pp3.device
+                                break
+                    _src = _donor.detach().to(device=_tgt, dtype=_lw.dtype)
+                    model.lm_head.weight = nn.Parameter(_src, requires_grad=False)
+                    _fin = bool(torch.isfinite(_src.detach().cpu()).all())
+                    print("[LMHEAD-FIX] OK donor=" + _dn + " dev=" + str(_src.device)
+                          + " shape=" + str(tuple(_src.shape)) + " dtype=" + str(_src.dtype)
+                          + " finite=" + str(_fin), flush=True)
+    except Exception as _e:
+        print("[LMHEAD-FIX] FAIL " + repr(_e), flush=True)
 
     # eval kv_cache ppl
     if args.use_ppl_eval_for_kv_cache:
